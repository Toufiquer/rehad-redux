import { createSlice } from "@reduxjs/toolkit";

const initialState = {};
const quizzesMarksSlice = createSlice({
  name: "quizzesMarks",
  initialState,
  reducers: {},
});
export const {} = quizzesMarksSlice.actions;
export default quizzesMarksSlice.reducer;
